# rag-platform 面试点速查

> 企业知识库 RAG 问答平台（求职作品集）。本文档把项目里每个工程决策背后的"面试考点"汇总成一份可背诵、可迁移的清单。
> 结构：**面试七问总览 → 按里程碑展开 → 高频追问与应对 → 一句话话术**。

---

## 一、面试七问总览（面试官只会关心这七个）

| # | 面试官会问 | 本项目怎么答 | 落在哪个里程碑 |
|---|---|---|---|
| 1 | 回答质量怎么度量？ | 黄金评测集 + RAGAS 指标 + 消融实验 | W4 |
| 2 | 多部门数据权限怎么隔离？ | 多租户 + 文档级/片段级权限（元数据过滤） | W3 |
| 3 | 文档更新/删除会过时吗？ | 文档版本管理 + 增量同步 | W3 |
| 4 | 没有答案的问题怎么处理？ | 无答案识别（no_answer） | W2 |
| 5 | 怎么防止胡编、让用户信服？ | 引文/来源标注 + 落地校验 | W2 / W2.5a |
| 6 | 线上坏回答怎么定位？ | 全链路追踪（Langfuse/OpenTelemetry） | W5 |
| 7 | 参数选择凭什么？ | 系统性消融实验 + 回归评测 | W4 |

> 讲项目的正确姿势：**不要按功能顺序讲，按这七问讲**。每答一个问题，就自然引出对应模块，显得你"懂业务问题 > 会写代码"。

---

## 二、W1 文档处理管线（已完成）

### 1. 异步 vs 同步处理文档
- **问题**：上传大文档卡住接口怎么办？
- **答案**：`INGESTION_MODE` 配置切换。开发用 `inline`（上传即处理，秒级复现）；生产用 Celery + Redis 队列，接口秒回，后台慢慢处理，前端轮询状态。
- **考点**：开发/生产配置分离；任务队列的基本思想（解耦耗时任务、失败重试）。

### 2. 环境变量命名冲突（实战踩坑）
- **问题**：`pymilvus` 一 import 就报 `Illegal uri`？
- **答案**：pymilvus 3.x 在 import 时会读**同名环境变量** `MILVUS_URI` 做服务器地址校验。我们的配置字段也叫 `MILVUS_URI`，值却是本地文件路径，被它拿去当服务器地址 → 报错。改成 `VECTOR_URI` 解决。
- **考点**：库会读环境变量（命名污染）；排查思路——"报错在 import 阶段，优先怀疑同名环境变量"。

### 3. 切片参数 chunk_size / chunk_overlap
- **问题**：长文档怎么切？
- **答案**：按字符切，块间重叠避免语义被切断（500 字 / 重叠 50）。重叠让同一段内容出现在多个切片，牺牲一点存储换召回率。
- **考点**：为什么重叠？——边界句子常被切断导致语义丢失；参数留成配置，因为 W4 消融实验要调它。

### 4. 向量元数据设计 = 为权限埋的伏笔
- **问题**：入库时为什么每条向量都带 `department` 标签？
- **答案**：多租户权限靠**检索层的元数据过滤**实现（Milvus 标量过滤），所以 W1 建表时就埋好标签，W3 直接能用。
- **考点**：数据结构要为后续需求提前设计——"权限过滤发生在检索层还是应用层"是面试高频题。

### 5. 内容去重（content_hash）
- **问题**：同一份文档上传两次？
- **答案**：SHA-256 内容哈希 + 部门维度去重，重复上传直接返回已有文档。
- **考点**：哈希去重的选型（不是主键判断，是内容判断）；为 W3 文档版本管理打底。

### 6. 为什么先 Mock 向量后接真实模型
- **答案**：Mock 向量用文本哈希做种子生成**确定性**向量，同一文本永远同一向量 → 全流程可复现、可测试、零成本。接口抽象（`EmbeddingProvider`），之后换 API/本地模型只改一处。
- **考点**："先跑通链路，再升级组件"的开发策略；可替换接口（依赖倒置）。

---

## 三、W2 混合检索 + RAG 问答 + 引文（已完成）

### 7. 为什么混合检索优于纯向量检索
- **答案**：**Dense（向量）**擅长语义相似——问"怎么休年假"能命中"带薪年假"；但精确词、专名、代码会漂移。**Sparse（BM25）**保证精确词命中——问"报销上限"能精确到报销章；但理解不了同义词。两者互补。
- **考点**：说出"互补"和具体例子，比背定义强得多。

### 8. RRF 为什么用"排名"不用"分数"
- **答案**：不同检索器的分数（余弦距离 vs BM25 打分）量纲不同、不可直接比较；但**排名可比**。RRF 公式：`score(c) = Σ 1/(k + rank_c)`，k 通常取 60（平滑常数）。
- **考点**：这是经典面试题，公式要能默写。

### 9. 中文分词
- **答案**：BM25 需要词级 token，中文没有空格，用 jieba 分词。
- **考点**：中文 NLP 的工程细节；提到"分词的粒度影响检索质量"说明你想过。

### 10. 引文为什么是企业级 RAG 的门槛
- **答案**：让每条回答能溯源到具体切片 → 降低幻觉、可审计、用户才信。实现：模型输出带 `[编号]` 标记，服务端正则提取 → 映射回切片 → 填 `Citation`。
- **考点**：这是"七问"里"怎么防止胡编"的答案，面试官极看重。能讲"回答必须带引文"这个**产品设计**就赢了。

### 11. no_answer 是开放难点（实战踩坑）
- **答案**：Mock 阶段用"问题字符与切片重叠度"判断（阈值 0.25），但会误判——"推荐一家附近的餐厅"撞上资料里报销章的"餐饮/附发票"。真实方案：**LLM 自己判断 + 检索分数阈值**。
- **考点**：承认这是难点、讲清楚为什么难（语义相近但资料里没有），比硬说"我解决了"更可信。RAG 的 no_answer 学界都没完全解决。

### 12. 权限过滤在检索层做，不是检索后过滤
- **答案**：BM25 在 SQL join 上按 `department` 过滤；向量在 Milvus 用元数据过滤表达式。先过滤 → 省算力 + 更安全（不相关数据不进后续环节）。
- **考点**："过滤前置"的安全设计思想。

### 12.5 关键词检索怎么撑住十万级文档（性能改造）
- **答案**：原实现 `select(Chunk)` 全表拉进内存再逐条算 BM25，O(N) 随文档量线性劣化。改成**数据库倒排索引**：开发/测试用 SQLite FTS5（bm25() 排名），生产用 PostgreSQL tsvector + GIN 表达式索引（ts_rank()），一个 `SparseIndex` 接口按 `DATABASE_URL` 返回实现，代码零改动。中文仍用 jieba 空格拼接入索引，召回语义与旧 BM25 对齐（回归评测不退化）。
- **考点**：面试主动讲"检索层做了性能改造、O(N)→O(log N)、双后端统一接口"，是区分 Demo 和工程化的点。

### 13. 切片重叠导致一段内容跨多个切片
- **答案**：同一个句子可能同时在 chunk1 和 chunk2 里，所以"被引 chunk 开头像整篇开头"不一定是错。
- **考点**：引申出**引文落地校验**（核对被引切片是否真含答案句子）——生产必做，见 §16。

---

## 四、W2.5a 真实 LLM 接入（已完成）

### 14. OpenAI 兼容协议 = 多 Provider 抽象
- **答案**：DeepSeek / 通义 / Qwen / OpenAI 用同一套接口协议（`/chat/completions`），所以我们的 `OpenAICompatibleLLM` 只差 `base_url`、`api_key`、`model` 三个配置即可切换。生产里不绑死单一厂商。
- **考点**：企业常问"怎么避免厂商锁定"——兼容协议 + 配置化切换就是答案。

### 15. no_answer 哨兵句
- **答案**：提示词里约定"资料里没答案就回答固定短语"，实现里检测该短语判定 no_answer。轻量方案，不用解析 JSON 结构化输出。
- **考点**：提示词工程（约束输出格式）+ 轻量解析的结合。哨兵句比"让模型自由发挥"可控得多。

### 16. 引文编号不能全信（加分项）
- **答案**：模型有时会标错编号（引用了 [2] 但内容在 [1]）。生产做法是**引文落地校验**：检查被引切片是否真的包含答案中的句子/实体。
- **考点**：这是大多数人不知道的进阶点，主动讲出来面试官会眼前一亮。

### 17. Mock 保留作回归对照
- **答案**：`LLM_BACKEND=mock|api` 随时切换。Mock 确定性强，W4 评测时可以作为基准（消融实验的对照组）。
- **考点**：可测试性设计；评测里"对照实验"的思想（见 §31）。

---

## 五、W2.5b 流式输出 SSE（已完成）

### 18. SSE vs WebSocket 怎么选
- **答案**：AI 回答是**服务器单向推给浏览器**，SSE 正好——基于 HTTP、浏览器原生支持、自动重连、实现简单。WebSocket 是双向的，适合聊天、实时协作等需要客户端也主动推的场景。
- **考点**：经典技术选型题，能说出"按通信方向选型"就对了。

### 19. SSE 事件协议设计（meta/delta/done）
- **答案**：三个事件把 RAG 流程透明化：
  - `meta`：先推检索到的资料 → 前端显示"已检索到 3 条资料，正在生成…"
  - `delta`：模型真实逐字输出（DeepSeek 原生流式）
  - `done`：完整回答 + 引文 + no_answer
- **考点**：为什么先推 meta？——让用户看到"先检索、再生成"，不是黑盒。**流式协议的分层设计**是加分点。

### 20. 为什么 axios 不能流式，必须用 fetch
- **答案**：axios 是"等完整响应才返回"；SSE 是"响应头先返回，body 持续吐"。前端用原生 `fetch + ReadableStream` 逐段读，按空行切事件块。
- **考点**：HTTP 流式响应的本质；对前端网络层的理解。

---

## 六、W2.5c Rerank 重排（已完成）

### 21. 召回 vs 重排的区别
- **答案**：**召回**用 bi-encoder（问题、文档各自编码成向量算相似度），可提前建索引、快但粗；**重排**用 cross-encoder（问题和文档**拼接**一起过模型打分），精但慢，所以只对召回的前 N 条重排。
- **考点**：bi-encoder vs cross-encoder 是必考题；"先粗后精"的分阶段检索范式。

### 22. 为什么只对 top-k 重排
- **答案**：cross-encoder 是两两拼接过模型，成本 O(n)；全文重排不现实。所以召回（快） → 重排（慢但精） → 生成，三层管线。本项目 `RERANK_RECALL_K=20` 召回，重排后取 `RERANK_TOP_N=5`。
- **考点**：工程上的算力/质量权衡。

### 22a. 接口抽象，真模型可换（实战）
- **答案**：`RerankerProvider` 抽象 + `get_reranker_provider()` 工厂，`RERANKER_BACKEND=lexical|api|none` 切换。lexical 是轻量词法（查询词覆盖率 + 命中位置加分），离线免费；`api` 是真实 cross-encoder（SiliconFlow bge-reranker-v2-m3），生产默认；`none` 留给 W4 消融对比"有重排 vs 无重排"。接入真模型只改 factory 一处——抽象的价值兑现。
- **考点**：mock-first 策略的完整闭环：先词法跑通链路，再换真实 cross-encoder，实测把 context_precision 从 0.94 拉到 1.0。

### 22b. 诚实说明轻量重排的局限（加分反例）
- **答案**：词法信号和 BM25 有重叠，提升有限（旧版实测有"把不相关块排上来"的反例）——所以从没拿它吹"重排提升了 X%"；真正拉开差距的必须是 cross-encoder（同时看问题+文档的每个词互动）。换真实 bge-reranker-v2-m3 后，实测把无重排漏掉的 2 道题（cp 0.5）修正到 1.0。
- **考点**：知道工具局限在哪 **并真的去换掉它**，是最值钱的工程认知闭环。

---

## 七、W3 多租户权限（已完成）

### 23. 权限 = 数据 × 规则 × 执行（三层框架）
- **答案**：任何权限系统都是这套框架——**你是谁**（数据：角色 role + 部门 department，W1 建表就埋好了） × **你能干什么**（规则：角色×操作矩阵） × **在哪拦**（执行：后端接口拦截，不是前端藏）。
- **考点**：能讲出这个可迁移框架（换到电商后台、内容管理都一样），比堆术语强。面试官常考"权限过滤发生在检索层还是应用层"——本项目是**检索层过滤前置**（Milvus 元数据过滤 + SQL join 过滤），不是检索完再过滤。

### 24. 部门参数由服务端解析，不能信前端
- **答案**：上传接口允许带 `department` 参数，但**只有 admin 能指定任意部门**；manager 传别的部门直接 403。部门的**最终归属是服务端根据角色算出来的**（`_resolve_upload_department`），不是前端传啥存啥——否则任何人都能越权建文档。
- **考点**：安全原则"永远不相信用户输入"，特别是权限相关的输入。这是面试官挖坑的高发点。

### 25. 删除文档必须联动清理向量库（双存储一致性）
- **答案**：系统是双存储——数据库存切片原文，Milvus 存向量。删文档只删一边，另一边就是"幽灵数据"：下次检索还能搜出已删除的内容。实现：删 DB 行之前，先按该文档的 chunk_id 调 `delete_by_chunk_ids` 清 Milvus。
- **考点**：跨系统一致性是最高频的工程面试题之一（删缓存、删索引、删文件同理）。能主动讲"先删向量再删库，不留幽灵数据"是实打实的工程意识。

### 26. 前端权限三层：菜单隐藏 + 路由守卫 + 按钮隐藏
- **答案**：前端只在"给人看"的层面做权限——菜单按角色显示、`/admin` 路由守卫（直接输 URL 也拦）、上传/删除按钮按角色隐藏。但**前端是装饰，安全在后端**：前端代码在用户浏览器里，改代码就能绕过，所以每个接口都挂了 `require_role`。
- **考点**："前端权限只是体验，后端权限才是安全"——反着说（以为前端拦住就够了）就是送命题。

### 27. 越权测试 = 负向用例证明权限
- **答案**：9 个 `test_permissions.py` 用例专门测"不该通的必须不通"：member 上传/删除 403、manager 指定别部门 403、manager 删别部门文档 403、member 只能看到本部门文档列表、非 admin 看用户列表 403；正向用例：admin 建号指定角色、admin 跨部门上传。测试用独立库（`test_rag.db` + `test_milvus.db`），每个用例前重建，不污染开发数据。
- **考点**：安全功能**必须配负向用例**，面试官问"你怎么证明权限生效"就指测试文件。测试隔离（不污染开发库）本身也是加分细节。

---

## 八、W4 评测体系（已完成，杀手锏）

### 28. 为什么评测是杀手锏
- **答案**：绝大多数作品集项目**没有评测**，回答好不好全凭感觉。评测体系让"好/坏"变成**可量化、可对比**的数字——这是和普通候选人的分水岭。本项目 `backend/eval_reports/ablation_report.md` 就是这张能拿出来讲的数字表。

### 29. 黄金评测集：把"标准答案出处"当 ground truth
- **答案**：`eval_data/golden_set.json` 手写 18 道「问题 + 标准答案出处」，每条 `golden_passage` 能从演示文档逐字命中。检索相关不靠模型判断，而是**查答案出处在不在这条切片里**——可解释、可复现、零成本。
- **考点**：评测集怎么来？这是面试高频题。"从自己项目的真实文档里抽问题 + 标注出处"是标准做法，比拿通用 benchmark 更有说服力（证明你懂业务）。

### 30. 手写 RAGAS 风格四指标（讲得清 faithfulness 怎么算）
- **答案**：四个指标分两组：
  - **检索质量**（不依赖 LLM，黄金答案出处直接算）：`context_recall`（出处被检索到没）+ `context_precision`（检索回来的切片里多少条相关，且相关切片排得越靠前分越高）。
  - **生成质量**（LLM 当裁判）：`faithfulness`（把回答拆句 → 逐句问 LLM"这句被资料支持吗" → 支持占比）+ `answer_relevancy`（直接让 LLM 给切题度打分 0~1）。
- **考点**：能说出"faithfulness 怎么算"（拆句 → 逐句判断是否被资料支持）就是加分项。**手写而不装官方 ragas 库**的理由也要能讲："库帮我算，我说不清原理；手写能讲清，还能控制裁判 prompt。"
- **踩坑**：LLM 裁判的输出解析要稳（只认 `1/0` 行，解析失败给中性分 0.5，不拖垮整轮）。

### 31. 消融实验：独立子进程控制变量
- **答案**：`scripts/ablation.py` 把「纯向量 vs 混合检索」「有重排 vs 无重排」「chunk 500 vs 300」每组配置**跑在独立子进程里**，用环境变量注入参数（`SEARCH_MODE` / `RERANKER_BACKEND` / `CHUNK_SIZE`），保证只改一个变量。
- **考点**：为什么子进程？——配置在模块 import 时就固定（`settings` 是单例），进程内改配置会串扰；子进程天然隔离，这是"控制变量"的工程实现。这回答七问 #7"参数选择凭什么"——"我们做了消融实验，数据说话"。
- **实测（全真实模式）**：嵌入 bge-m3 + 重排 bge-reranker-v2-m3（SiliconFlow）+ DeepSeek 裁判。纯向量 0.94 / 混合+重排 1.0、无重排 0.94 / 有重排 1.0、chunk500 与 chunk300 均有重排下 1.0（context_precision）。

### 32. mock → 真实嵌入的诚实演进（加分闭环）
- **答案**：开发期用 mock 哈希嵌入（无语义）免费跑通全链路，mock 下"纯向量 0.21"是**占位结论**——只证明方法论和相对对比，不代表真实水平（报告当时明确标注"mock 模式"）。切真实 bge-m3 后重跑，**纯向量 0.21 → 0.94、混合+重排 → 1.0**。占位与真实的差异本身就是最好的验证。
- **考点**：
  - 主动说"哪个数字在什么条件下有效、什么时候要重跑"，比拿一张貌似权威的数字表更能赢信任——面试官最烦不懂边界地吹数据。
  - 更难得的是**闭环**：不是停在"这是 mock 的所以别信"，而是真去把生产模式跑完、给出真实数。这两步连起来讲，就是"我知道方法的边界，并且把它做完了"。
  - 边界要主动讲：满分会受语料大小影响，所以我后来把语料升到 8 份带脏数据的企业文档、评测升到 52+3 题（§59/§60）——在脏文档上拿 1.0 比干净小语料的 1.0 含金量高，但仍然要诚实：更大语料才能拉开配置差距。

---

## 九、W5 工程规范（已完成）

### 33. 可观测性：request_id 全链路追踪
- **答案**：纯 ASGI 中间件给每个请求分配 request_id，用 contextvars 注入当前协程，`setLogRecordFactory` 让**每一条日志**都带上 rid（不用给函数签名传参）。响应头回传 X-Request-ID，访问日志记录 method/path/status/耗时。线上坏回答 → 拿响应里的 X-Request-ID → grep 日志把整条链（鉴权→检索→LLM→响应）捞出来。
- **考点**：
  - 为什么用纯 ASGI 而不是 `BaseHTTPMiddleware`？后者会**缓存整个响应体**，把 SSE 流式输出打断——手写中间件才踩得到的坑。
  - 为什么用 `setLogRecordFactory` 而不是 logger.addFilter？Filter 只对经过某些 logger 的记录生效，第三方库（如 milvus-lite）直接打的日志会漏，一格式化就崩 `Formatting field not found`。工厂方法保证**所有**记录都有 rid。
  - 透传上游 X-Request-ID = 分布式追踪的接入口（网关分配的 trace 一路带到底）。

### 34. 限流：手写令牌桶保护 LLM 接口
- **答案**：`/chat` 每次都是真金白银的 API 调用（W4 评测就意外花过钱）。令牌桶：稳态速率注令牌、容量允许突发、拒绝时 429 + `Retry-After`。每个用户独立桶，先鉴权再限流。单机内存版（asyncio.Lock 保护），生产多副本换 Redis 分布式版，思路不变。
- **考点**：
  - 手写令牌桶能讲清"容量=突发、注水速率=稳态"比直接拿 slowapi 更有说服力（项目一贯风格：手写讲得清原理）。
  - 为什么限流在鉴权**之后**？未登录请求根本到不了限流层，不给爬虫计数。
  - 429 必须带 `Retry-After`（HTTP 规范），客户端知道等多久。

### 35. CI：测试与静态检查的门禁
- **答案**：GitHub Actions 两个 job——`pytest`（SQLite + Milvus Lite 本地文件，无需外部服务，Ubuntu runner 直接跑）+ `ruff` 静态检查。push/PR 自动触发，测试不过不让合。
- **考点**：
  - 测试能在 CI 无 Docker 直接跑，是因为隔离设计从测试第一天就做好了（conftest 强制独立库 + mock）——"CI 跑得动"是设计出来的，不是运气。
  - ruff 规则是**刻意挑过的**：中文全角标点冲突的 RUF001-3、HTTP 状态码魔法数字 PLR2004、惰性 import PLC0415、StrEnum/PEP695 风格偏好 UP042/46，都明确 ignore 并写理由。"为什么配这些规则"本身也是加分点（说明不是无脑上 lint）。

---

## 十、W6 边界打磨（已完成）

### 36. 外部依赖失败兜底：分层异常处理
- **答案**：三层兜底——`AppError` 业务错误转 4xx 统一 JSON；httpx 错误（LLM/Milvus 超时、连接失败）转 502（`UPSTREAM_ERROR` / `UPSTREAM_UNAVAILABLE`）；最后一道 `Exception` 兜底转 500 统一 JSON。真实堆栈进日志，用户只看到友好提示。
- **考点**：
  - "对用户友好、对排查诚实"是生产系统的默认姿态，裸 500 是 demo 级表现。
  - `httpx.HTTPStatusError` 是 `HTTPError` 的直接子类、**不是** `RequestError` 子类——两个 handler 各管各的，别把 `raise_for_status()` 报错当网络错误。
  - 全局 handler 按异常 MRO 找最具体的注册项，所以通用 `Exception` 兜底不会吃掉 AppError / 422 / 401。

### 36a. 外部依赖挂了先自救再兜底：可恢复错误重试（W9）
- **答案**：W6 做到"挂了优雅返回 502"，W9 更进一步——瞬时故障先重试再兜底。统一 `retry`/`async_retry` 策略（LLM/Embedding/Rerank 三个调用点同一套）：只重试可恢复错误（429 / 5xx / 网络层超时、连接重置），4xx 不重试（重放必失败）；指数退避 + 抖动防重试风暴，429 优先遵循服务端 `Retry-After`（按上限封顶）。重试耗尽抛错后自动落进 W6 的 502 分层（`UPSTREAM_ERROR` / `UPSTREAM_UNAVAILABLE`），零新增异常路径。**流式 SSE 刻意不重试**——内容已逐段吐给用户，重放会重复输出。
- **考点**：
  - "哪些错误值得重试、哪些重放也必失败"是判断工程成熟度的题：无脑全重试会把 4xx 的请求费再烧一遍。
  - 退避必须带抖动（jitter），否则多个并发客户端会同步重试、退避结束后同时打回来形成雪崩。
  - 流式与普通请求重试策略不同，能主动说出"流式不能安全重试"是加分项。
  - 重试与错误处理的关系：重试是"自救"，502 是"自救失败后的体面退场"——两层衔接而不是重复实现。

### 36b. 在线重试只是半边：离线入库任务的另一条重试路径（W10）
- **答案**：W9 只覆盖「在线问答路径」（HTTP 调用重试）；但「离线入库路径」是 Celery 任务，embedding 接口故障时任务直接失败、文档静默卡死。W10 给任务层加重试：复用 W9 的退避与状态码判断，`is_transient` 区分瞬时（嵌入 API 429/5xx、网络层故障 → 指数退避自动重试，最多 3 次尝试）vs 永久（解析失败等 → 直接失败）；`failure_reason` 落库，前端悬停可见；失败文档可 `POST /documents/{id}/retry` 手动重新入队（仅 manager/admin、仅 failed 状态、部门隔离）。
- **考点**：
  - "在线重试 vs 离线重试"是外部依赖容错的完整叙事：W9 管用户等待的那条路，W10 管后台异步的那条路，两条路径的重试哲学一致（瞬时重试、永久放弃）。
  - 任务重试复用 W9 的退避函数和可重试状态码集合，而不是各写一套——单一来源（single source of truth）。
  - 失败原因落库 + 手动重试，是把"静默失败"变成"可见、可恢复"——生产上传不能无声丢文件。
  - 重试次数要有上限（这里 3 次尝试），否则上游持续故障时任务无限重跑空耗资源。

### 37. 文件上传安全边界（OWASP 必考）
- **答案**：类型**白名单**（`.txt/.md/.pdf/.doc/.docx`）+ 大小上限（20MB，**分块读取**）+ 空文件拒绝。
- **考点**：
  - 为什么用白名单不用黑名单：黑名单要枚举所有恶意类型，永远补不齐。
  - 大小限制要在**读取时**卡（分块累加、超限即断），而不是读完整包再检查——否则超大文件已经把内存打爆了，限制失去意义。

### 38. API 层输入校验：不能只信前端
- **答案**：`ChatRequest.question` 用 `field_validator` 做 strip 后非空校验，空问题/纯空格返回 422。
- **考点**：校验放 API 层的本质是**信任边界**——浏览器不是可信边界，绕过前端直接调 API 很容易，后端必须自己验证每份输入。

### 39. 文档版本管理：更新一致性怎么保证
- **答案**：同部门同文件名重传 = 版本更新。先查同文件名文档：存在且内容相同 → 拒绝（"已是最新版本"）；存在且内容不同 → 版本 +1，**先清旧切片（Milvus 向量 + DB 记录）再灌新内容**；不存在 → 新文档。处理中的文档拒绝更新（防异步任务竞态）。
- **考点**：
  - 为什么"先清后灌"而不是直接追加？新旧切片混库，回答会同时引用新旧内容，检索语义被污染。
  - 双存储一致性：删了 DB 不删向量会留"幽灵"数据（检索命中已删除文档）。删除和升版共用 `_delete_doc_chunks`，一处逻辑两处用。
  - 处理中的文档为什么不能更新？异步模式下旧任务可能晚于新任务完成，把旧内容标记成 ready，覆盖新状态——必须在入口拦截。
  - 面试追问"要不要保留历史版本"：当前是原地替换（简单、省空间）；生产可加 Chunk 版本字段做"写入新版本、原子切换、旧版可回滚"，是同一个思路的升级版，讲得出来就行。

---

## 十一、W6 作品化（规划）

### 40. 作品集怎么讲
- **答案**：README 面向面试官写（一页讲清七问）、录 1 分钟演示视频（上传 → 问答 → 引文 → 权限）、准备 3 个能深挖的亮点（评测 / 权限 / 引文落地校验）。
- **考点**：技术点可以讲不完，但**讲故事的逻辑**和**能证明的东西**要留足。

---

## 十二、W7 生产化验收（Docker 全栈）

### 41. 开发/生产双环境怎么做到"代码零改动"
- **答案**：同一套代码，只切环境变量就换底座——`DATABASE_URL`（sqlite+aiosqlite / postgresql+asyncpg）、`VECTOR_URI`（Milvus Lite 本地文件 / Milvus standalone http 地址）、`INGESTION_MODE`（inline / async）、`EMBEDDING_BACKEND`（mock / api）。开发秒级启动，生产全量服务，**配置驱动而非代码分支**。
- **考点**：这是"生产化"最基本的素养——写代码时就把环境差异收敛到配置层，而不是到处 if 环境。

### 42. 为什么代码里一直躲着 MILVUS_URI 这个环境变量名
- **答案**：pymilvus import 时会读**同名环境变量** `MILVUS_URI` 做校验，我们的配置字段也这么叫就会撞车（开发值是本地文件路径，被拿去当服务器地址）。所以统一用 `VECTOR_URI`。
- **考点**：见 W1 §2。生产 compose 里 `environment: VECTOR_URI=http://milvus:19530` 也直接覆盖，不用改代码。

### 43. compose 编排：健康检查依赖链（实跑踩坑）
- **答案**：`depends_on` + `condition: service_healthy`。完整链：etcd（`etcdctl endpoint health`）+ MinIO（curl `/minio/health/live`）健康 → Milvus（curl `/healthz`）健康 → backend/worker（等 postgres/redis 健康）。**实跑踩到的坑**：etcd 镜像默认只监听 localhost，容器网络里 Milvus 连不上（而 etcd 的健康检查查自己内部 localhost 照样"健康"）——必须显式 `command: etcd -advertise-client-urls=http://etcd:2379 -listen-client-urls=http://0.0.0.0:2379 --data-dir /etcd`；否则 Milvus 启动 5 秒连不上 etcd 直接 panic（exit 134）。
- **考点**：裸 `depends_on` 只保证"启动了"不保证"可用了"；**健康检查也可能被"localhost 假健康"骗过**——要检查"别人连你"的地址，而不是自检。

### 44. Celery 异步管线：接口秒回 + 任务不丢（实测）
- **答案**：上传接口只 `process_document_task.delay(doc.id)` 投递到 Redis，返回 200 立即收工；worker 后台执行 parse→chunk→嵌入→入库，前端轮询状态 pending→ready。`task_acks_late=True`（执行完再 ack，worker 崩溃不丢任务）+ `worker_prefetch_multiplier=1`（一次只取一个，不堆积）。**实测**：上传 828 字节文档，worker 日志 `documents.process received → succeeded in ~2s`（含真实 bge-m3 嵌入），文档状态 pending→ready。
- **考点**：异步解耦耗时任务的思想；"先回 200 再后台干"的体验设计。`acks_late` 是有真实取舍的工程细节（多一次投递换可靠性）。

### 45. 密钥管理：为什么 .dockerignore 是生产红线
- **答案**：`backend/.dockerignore` 排除 `.env`/`data/`/`tests/`，真实 API key 永远**不进镜像层**（`docker inspect` 翻不出）；运行时经 compose `env_file: [.env, backend/.env]` 注入，compose `environment:` 再覆盖开发默认值（`INGESTION_MODE=async` 等）。三层 env 优先级：`environment > 后一个 env_file > 前一个 env_file`。
- **考点**：镜像 = 可分发制品，密钥烤进去等于公开。这是"生产级"和"demo 级"的直观分界线，也是安全面试高频点。

### 46. nginx SSE 反代的坑：答案会"憋"到结束才来
- **答案**：nginx 默认缓冲上游响应，SSE 流式会被它攒整段再吐给浏览器 → 用户体验是"半天没反应"。反代 `/chat/stream` 必须 `proxy_buffering off` + `proxy_http_version 1.1` + `proxy_set_header Connection ''` + 长 `proxy_read_timeout`。
- **考点**：中间件默认行为（缓冲）会破坏流式协议——不是配了 SSE 就完事，还要让每一层都放行。实测踩到才讲得出来。

### 47. 数据迁移与重灌：seed 脚本干了什么
- **答案**：`scripts/seed_dev.py` 重建表（drop_all + create_all）+ 重建 Milvus 集合 + 建三角色账号（admin/mgr_hr/member_hr）+ 用**真实 bge-m3** 走完整管线重灌 8 份演示文档（inline 直调 `process_document`，不受 INGESTION_MODE 影响）。这是把"开发数据"变成"可复现演示数据"的标准做法。
- **考点**：演示前能一键重建数据 = 数据资产化；也顺带验证了生产向量库的建集/写入/检索链路。

### 48. 境内网络：Docker Hub 直连超时怎么办（实测）
- **答案**：`~/.docker/daemon.json` 配 `registry-mirrors`（daocloud 等），重启 Docker Desktop 生效。quay.io 的 etcd 直连可用，docker.io 的镜像一律走镜像源。**构建阶段同样要配**：`backend/Dockerfile` 设 `PIP_INDEX_URL`（清华源）、`frontend/Dockerfile` 加 `npm config set registry`（阿里镜像）——否则 pip 直连 PyPI 单步能卡 20 分钟+（实测卡死重建），配镜像后 1-3 分钟装完。
- **考点**：基建可用性是工程现实问题；"不是不能跑，是网络层的问题 + 配置层解决"就体现了动手能力。

### 49. 全栈实跑验收：8 容器全链路（W7 成果）
- **答案**：`docker compose up -d --build` 一键拉起 8 个容器（postgres/redis/etcd/minio/milvus/backend/worker/frontend），健康检查链全绿；生产 seed 用**真实 bge-m3** 灌 8 份演示文档（76 切片 / 1024 维向量入 PostgreSQL + Milvus standalone）；E2E 全过：注册→登录→SSE 问答（经 nginx 反代逐字推送，真实 DeepSeek + 引文）→会话→改密（旧密码立即失效 401）→越权防护（member 删文档 403 / admin 200）→删除联动清双存储。脏文档升级后重跑：试用期/差旅标准/T4 月薪/离职年假补偿（跨文档拼答）/经理级加班费（藏在提示块里的边界条款）/无关问题拒答——全部带引文答对。**验收前必须停掉遗留 dev 进程**（vite dev server / 本地后端会占 localhost 端口，把生产栈挡在后面）。
- **考点**：验收不是"代码能跑"，是**生产路径真的在跑**——中间件层（nginx 反代、SSE 缓冲）也都实跑验证过，数字和日志拿得出来。

---

## 十三、W11 回答缓存（已完成）

### 50. 语义缓存：热问题秒回怎么不把权限泄露出去（W11）
- **答案**：问答走「语义缓存」——问句向量化后在 Milvus `question_cache` 集合找相似问句（余弦 ≥ 0.95 命中），命中就从 Redis 直接拿回答（含引文），不再调 LLM：热问题秒回、省 API 费，换种问法也能命中。两个必须处理的坑：①**权限隔离**——缓存键带 department，因为回答依赖该部门可见文档，键带部门才不会跨部门串答；②**失效**——每部门一个「知识库版本号」（Redis 计数器），文档增/删/改/重试时 +1，缓存负载记录生成时版本，版本不一致即 miss。粗粒度但简单可靠，再配 TTL（24h）双保险。KV 可插拔（redis | memory），生产 Redis、测试内存；全部读写 fail-open——缓存是优化不是依赖，Redis/Milvus 挂了问答照常走。
- **考点**：
  - 语义缓存 vs 精确缓存：精确缓存键是问句原文，换几个字就 miss；语义缓存用向量，相近问法也能命中（真实嵌入 bge-m3）。mock 嵌入退化为精确匹配，是诚实可讲的降级路径。
  - 缓存"回答"等于缓存了"知识快照"，版本失效是正确性命门——能主动讲"版本号 + TTL 双保险"就说明考虑过过期。
  - 权限是缓存最容易踩的坑：无脑用"问题"当键，同问不同部门会串。键带 department 是把"检索权限"延伸到了"缓存权限"。
  - 流式也要接缓存：命中时整个回答一次推完（单 delta 秒回），前端无感；未命中照常流式、结束后回填。

---

## 十四、企业级加固（P1/P2 工程化）

### 51. 跨存储一致性：外部存储不参与事务，怎么保证不出脏数据（P1-1）
- **答案**：Milvus / 稀疏索引 / Redis 都不参与 DB 事务，崩溃或重试会留孤儿数据（有向量没 DB 切片）。做法是「**DB 是事实来源 + 对账补偿**」：入库顺序固定为「DB 先提交 → Milvus 后写 → 置 ready」，失败就清掉已提交的 DB 切片（失败文档绝不能还检索得到）；Milvus 写入按文档幂等（先删旧行再插入）；再配一个**对账任务**（admin 端点 + Celery 周期），以 DB 为准清孤儿向量、报告缺向量。
- **考点**：
  - 能主动说出"外部存储无法参与事务，所以用状态机 + 对账保证最终一致"——这是和 demo"看起来能跑"最直观的分水岭。
  - 失败顺序有讲究：Milvus 写失败时 DB 已提交切片，若不清理，失败文档的内容仍能被检索到——数据污染比多一条孤儿向量更可怕。
  - 对账是**幂等收敛**而非一次性修复：清孤儿、报缺失，可重复跑，是生产兜底该有的形态。

### 52. 无状态 JWT 怎么实现登出 + 登录防爆破（P1-3）
- **答案**：JWT 无状态、无法主动失效，登出靠**黑名单**：签发时带 `jti`（唯一 id），登出时把 jti 拉黑到 token 过期为止，校验时查黑名单命中即 401——登出后旧令牌立即失效。登录按「用户名+IP」令牌桶限流，超限 429 + Retry-After 防暴力破解；生产环境占位 `SECRET_KEY` 直接拒绝启动。
- **考点**：
  - "无状态 JWT 怎么踢人"是 JWT 必考题，能讲黑名单（而非只讲刷新令牌）说明懂权衡：牺牲一点点无状态性换可控性。
  - 黑名单是安全冗余而非依赖：Redis 挂了只影响"登出即时性"，校验查不到就当有效放行——安全组件也要 fail-open 有度。
  - 登录防爆破按 **用户名+IP** 组合限，而不是只按 IP（共享出口 IP 会误伤一栋楼）。

### 53. 审计日志：为什么"能删数据不留痕"进不了企业（P1-5）
- **答案**：`audit_logs` 表记录 谁（actor + 冗余用户名）/ 何时 / 对什么对象 / 做了什么 / 详情，与业务**同事务**提交——操作即留痕，没有"做了但没记"的窗口。覆盖文档上传/升版/删除/重试；admin 可按操作人/动作分页查询。
- **考点**：审计日志是合规硬要求，"谁删了这份文档"必须可追溯。要点是同事务（不是事后异步补，会丢），以及**冗余存用户名**（用户被删后还能溯源）。

### 54. token 预算：LLM 成本是一等公民（P1-6）
- **答案**：给 LLM 的输入按 token 预算动态截断——检索切片带编号进上下文，超出 `MAX_CONTEXT_TOKENS` 的后段丢弃、重新编号；输出设 `max_tokens` 上限。顺带修了一个隐藏 bug：重排关闭时 20 条切片会全进 LLM（上下文溢出 + 烧钱）。
- **考点**：把 API 成本当架构约束来管，而不是事后看账单——"成本即架构"。token 计数用启发式（中文 1 字≈1、英文 4 字符≈1）零运行时依赖，精确可换 tiktoken，讲得出这个取舍。

### 55. 多轮对话：为什么追问有上下文、却要绕过缓存（P2-1）
- **答案**：把最近几轮历史（截断到预算内）喂给 LLM，追问（"那上限呢？"）不再被当孤立问题。**多轮请求不进回答缓存**——缓存键只有问句 + 部门，不含会话上下文，缓存会答非所问；单轮热问题继续走语义缓存。
- **考点**：
  - 能指出"history 字段接了但没参与生成"是半成品，说明你看得见功能边界。
  - 缓存与上下文的关系是深度追问点：缓存的是"知识快照"，多轮带的是"会话状态"，两者语义不同，硬合会串——主动讲清比被问倒强。

### 56. 质量护栏：评测进 CI，质量回退即红（P2-6）
- **答案**：CI 增加 eval job——每次提交离线跑黄金集评测（mock，不烧 API），与 committed 基线对比：任一指标跌过容差或 no_answer_rate 上升即 CI 红。
- **考点**："质量是每次提交都在守，不是上线时测一次"——这是和"写完就跑"的最大分水岭。基线 vs 容差（而不是硬阈值）意味着允许改进、拦截回退，是可持续的护栏。

### 57. 生产才暴露的 bug：为什么 SQLite 全绿、PG 一打中文就挂（实跑踩坑）
- **答案**：①PG 稀疏检索 `to_tsquery('simple', "'一线' OR '城市'")` 对中文词 + OR 组合报 `syntax error in tsquery`——测试全走 SQLite FTS5 路径，PG 的 tsquery 从未被真实中文查询打过；改 `websearch_to_tsquery` 正确生成 `'一线' | '城市'`。②seed 二次灌库撞 `chunks_fts` 主键——FTS 表不在 ORM 元数据里，`drop_all` 删不到，补显式清稀疏索引。
- **考点**：
  - "测试全绿但生产挂了"比"本来就没测"更有故事性：它证明你**跑了生产**，且能讲清双实现（SQLite/PG）的差异是测试盲区。
  - 教训落点：**测试库和生产库的方言差异**是经典盲区，能主动讲说明你懂"开发环境绿不等于生产绿"。

### 58. 非 root 容器 + 去掉 bind-mount：生产正确性和开发便利的取舍（P1-4）
- **答案**：后端镜像多阶段构建 + `USER appuser` 非 root 运行（降容器逃逸风险）；上传目录用命名卷，**去掉源码 bind-mount**——容器里跑的必须是构建时锁定的代码，不是宿主机随时变的文件。代价是改代码要重建镜像，这正是"生产正确形态"和"开发便利"的取舍。
- **考点**：能主动讲这个取舍（而不是只报"我改成了非 root"）说明你真想清楚了：bind-mount 是开发爽、生产错的形态，命名卷 + 重建才是可交付。

### 59. 为什么演示文档要故意"做脏"（评测集 v2）
- **答案**：真实企业知识库的文档大多又脏又乱——页眉页脚残留（"机密·第X页共X页"）、版本记录带手写批注、新旧标准对照没删干净（标"（旧）"）、OCR 乱码、错别字、半角全角标点混用、tab 对齐的"脏表格"、重复段落、TODO 待补、术语不统一（人力资源部/人事部/HR）。我把演示文档按这个标准写成 8 份互相引用的企业制度集，每份 8-11 切片。**排版干净的文档会让管线显得没难度**；在脏数据里还能检索到标准答案，才是管线真本事。
- **考点**：
  - 面试官常见的"如果文档很乱怎么办"——答案是"我的评测集本身就是脏的"，直接消掉这个问题。
  - 脏文档天然产出真实难题：跨文档交叉引用（离职年假补偿散在两份文档）、表格取值（T4 月薪）、新旧标准冲突（答案藏在"以最新为准"）、拒答（食堂菜谱无关）。
  - 术语不统一恰好验证了 jieba 分词 + 倒排索引的鲁棒性，不是靠干净文本喂出来的假高分。

### 60. 评测集分可答题/拒答题：reject_accuracy 为什么独立成指标（评测集 v2）
- **答案**：RAGAS 风格四指标只能度量"可答题"的召回与生成质量——context_recall 是"标准答案出处被检索到没有"，但**衡量不了"正确地不知道"**。所以我给评测集分两类：52 道可答题跑四指标，3 道无关题单独算拒答准确率（reject_accuracy=正确拒答数/拒答题数），不掺进四指标均值。CI 护栏同时守两条线：可答质量不得回退、拒答率不得下降。
- **考点**：主动指出"四指标度量不了知识边界"说明你真理解评测指标的设计边界，不是照抄 ragas。正确答案本身（拒绝回答无关问题）也是质量的一部分，而且**无关题在 mock 评测下极难做对**（mock 用字符重叠度近似相关性，自然中文问题很容易误判为有答案）——我靠精心构造语料外问题拿到 3/3，这个对比本身就是可讲的深度。

---

## 十五、高频追问与应对

| 追问 | 应对 |
|---|---|
| "这个项目你怎么保证它真的work？" | 三层证据：pytest 全量 105 通过 + 开发栈（SQLite/Milvus Lite）切片/向量/流式截图 + **生产栈实跑验收**（8 容器、真实 bge-m3 重灌、SSE 经 nginx、Celery 实测 2s） |
| "demo 和可交付企业的差距在哪？" | 讲 P1 加固四命门：跨存储一致性（DB 事实来源 + 对账）、认证安全（登出黑名单/登录防爆破/弱密钥拒启）、审计日志、质量护栏（token 预算 + CI 评测防回退）——并补一句"生产踩过两个测试全绿但 PG 挂掉的 bug"，证明真的跑了生产 |
| "引文会不会是模型编的编号？" | 讲引文落地校验（§16），说明我知道这个坑 |
| "为什么不用 LangChain？" | 手写各环节能讲清原理、可控、代码少；LangChain 抽象太重（可选：对标用 LangChain 的同类项目做对比） |
| "10 万份文档你怎么办？" | 生产切 Postgres + Milvus standalone + Celery 异步 + 分页检索（docker-compose 已实跑验收）+ 关键词检索走倒排索引（PG tsvector / SQLite FTS5 双实现，O(N)→O(log N)） |
| "怎么换向量模型/LLM？" | 两个工厂函数 `get_embedding_provider` / `get_llm_provider`，配置化切换 |
| "检索效果不好怎么排查？" | 先看召回（BM25/向量分别返回什么）→ 再看重排 → 再看 prompt，逐层二分定位 |
| "消融实验是 mock 嵌入，数字能信吗？" | 诚实说明：mock 只验证流程和检索侧相对对比（纯向量 < 混合、无重排 < 有重排）；切生产模式（bge-m3 + 真实 LLM 裁判）后重跑才是绝对指标 |
| "评测集从 18 题扩到 52+3 还全满分，是不是太水？" | 满分是在 8 份**带脏数据**的企业文档上拿的（页脚残留/OCR 乱码/新旧对照/错别字），比干净语料的 1.0 含金量高；且 3 道拒答题在 mock 下极难做对（字符重叠近似相关性，自然问题易误判），拿 3/3 说明知识边界设计是真的；可答/拒答分开度量，四指标不被拒答题拖累，各有各的满分 |

---

## 十六、一句话话术（每个里程碑背一句）

- **W1**：从上传到入库的四步管线，异步可切换，权限标签在建表时就埋好。
- **W2**：混合检索双路召回 + RRF 融合，回答必须带引文，敢说"不知道"。
- **W2.5a**：真实 LLM 通过兼容协议接入，多厂商可切换，no_answer 用哨兵句，引文做了落地校验。
- **W2.5b**：SSE 流式，先推检索结果再逐字生成，用户看到的是"先检索再生成"而不是黑盒。
- **W2.5c**：召回放宽到 20 再精排取前 5，重排接口抽象可换真模型；诚实的反例是"词法重排会把年假排到加班费上"。
- **W3**：权限=数据×规则×执行，部门由服务端解析不信前端，删文档联动清向量，9 个越权测试证明拦截。
- **W4**：黄金评测集 52 可答 + 3 拒答（8 份带脏数据的企业文档）当标准答案，手写 RAGAS 四指标（检索两维 + 生成两维）+ 独立拒答准确率，消融实验独立子进程控制变量，好坏用数字说话。
- **W5**：request_id 全链路追踪（纯 ASGI 中间件，坏回答按 id 一捞到底），手写令牌桶限流保护花钱的 LLM 接口（429+Retry-After），GitHub Actions 自动跑测试和 lint 当门禁。
- **W6**：LLM/向量库挂了返回友好 502 不裸崩，上传设类型白名单+大小上限+分块读取，API 层自己校验输入——异常输入和外部依赖打不穿系统；同文件名重传自动升版，旧切片双存储同步清理，文档不会过时。
- **W7**：Docker 全栈实跑验收——环境变量切底座代码零改动，健康检查依赖链（踩过 etcd 只监听 localhost 的坑），Celery 异步实测 2s 处理完，.dockerignore 把密钥挡在镜像外，nginx 关缓冲放行 SSE，seed 一键重灌真实 bge-m3 演示数据，8 容器全链路 E2E 通过。
- **W8**：关键词检索从"全表扫描 + 内存 BM25（O(N)）"换成数据库倒排索引（O(log N)）——SQLite FTS5 / PG tsvector 双后端同一接口，中文 jieba 分词语义对齐，回归评测不退化。
- **W9**：外部依赖（LLM/嵌入/重排）挂了先重试再兜底——只重试瞬时故障（429/5xx/网络错误），4xx 重放必失败不重试，指数退避带抖动防雪崩，429 尊重 Retry-After；重试耗尽自动落进 502 分层，流式 SSE 刻意不重试因为内容已吐给用户。
- **W10**：重试补全另一半——离线入库任务自动重试，瞬时错误（嵌入 429/5xx/网络）指数退避重试、永久错误（解析失败）直接失败，失败原因落库前端可见，失败文档一键手动重试（仅 manager/admin、部门隔离）。
- **W11**：回答加语义缓存——问句向量在 Milvus 找相似命中，Redis 秒回省 LLM 费；缓存键带部门防权限串答，文档一变更部门版本号自动失效，TTL 兜底不膨胀。
- **P1**：从 demo 到可交付的一轮补强——外部存储不参与事务，用「DB 事实来源 + 对账收敛」防脏数据；JWT 登出拉黑 + 登录防爆破 + 弱密钥拒启；审计日志责任可追溯；token 预算管成本；非 root 容器 + 命名卷去掉 bind-mount。生产踩坑：PG 中文 tsquery 语法错误、seed 二次灌库撞 FTS 主键——都是只会在生产暴露的 bug。
- **P2**：多轮对话真正进生成（追问有上下文、多轮绕缓存），Markdown 标题感知切片，CI 评测护栏让质量回退即红；演示文档升级成带制度编号/表格的企业级 .md，评测基准不漂移。

---

*维护：每完成一个里程碑，把实际踩到的坑补进对应章节（面试官更喜欢真实事故，而不是完美方案）。*
